import 'package:flutter/material.dart';

class PedidosHistory extends StatelessWidget {
  const PedidosHistory({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
